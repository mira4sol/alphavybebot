import { VybeOraclePriceMessage } from '@/types'

export const handleOraclePriceMessages = (message: VybeOraclePriceMessage) => {
  // Handle oracle price messages
  console.log('Oracle price message:', message)
}
